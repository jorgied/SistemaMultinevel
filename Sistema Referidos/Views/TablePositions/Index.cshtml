@model IEnumerable<Sistema_Referidos.Models.TablePosition>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Customer.CustomerName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Table.TableDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Customer.CustomerName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Table.TableDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTablePosition }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTablePosition }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTablePosition })
        </td>
    </tr>
}

</table>
